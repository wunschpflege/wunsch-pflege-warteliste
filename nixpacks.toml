# Nixpacks-Konfiguration fuer Railway
providers = ["node"]

[phases.setup]
nixPkgs = ["nodejs_20", "openssl"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
# Migrationen + Seed laufen vor dem Start (idempotent)
cmd = "npm run release && npm run start"
